.wrapper
  .left 
    .left__top 
      %p.name  
        = current_user.name
      %ul.menu
        %li.cog
          = link_to 'groups/new' do
            %i.fa.fa-edit{class:'icon'}
        %li.edit
          = link_to 'users/edit' do
            %i.fa.fa-cog{class:'icon'}
    .left__body
      - current_user.groups.each do |group|
        = link_to group_messages_path(group) do
          .group__name
            = group.name
          .group__message
            メッセージはまだありません。

      = render partial: "groups/group"
      
